{
    "name": "preact-prop-helpers-demo",
    "version": "0.0.1",
    "type": "module",
    "description": "Demo page for preact-prop-helpers",
    "source": "index.tsx",
    "main": "bundle.js",
    "packageManager": "pnpm@8.6.11",
    "browserslist": "defaults",
    "scripts": {
        "watch": "npx esbuild ./index.tsx --define:process.env.NODE_ENV=\\\"development\\\"  --bundle --outfile=bundle.js --format=iife --sourcemap --minify-identifiers --minify-syntax --watch",
        "build": "npx esbuild ./index.tsx --define:process.env.NODE_ENV=\\\"development\\\"  --bundle --outfile=bundle.js --format=iife --sourcemap --minify-identifiers --minify-syntax --analyze",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "esbuild": "^0.18.18"
    },
    "dependencies": {
        "preact": "^10.16.0"
    }
}
